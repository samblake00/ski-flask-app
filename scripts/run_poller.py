# This script used to run the poller once for local development.
# It's been intentionally stubbed during cleanup. If you want to run the poller,
# call `from app.tasks import poll_providers` inside a running app context.

print('run_poller.py is stubbed; run poll_providers() from an app context if needed.')
