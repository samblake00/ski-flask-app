# This script used to inspect the development SQLite DB.
# It's been stubbed as part of cleanup. Use the Flask shell or a small script
# that imports the app and models if you need to inspect the DB.

print('check_db.py is stubbed; use Flask shell or admin tooling to inspect the DB.')
